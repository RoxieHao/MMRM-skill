options(encoding = "UTF-8")
find_root <- function(path) { current <- normalizePath(path, winslash = "/", mustWork = TRUE); repeat { if (file.exists(file.path(current, ".codex", "study-mmrm-analysis", "R", "analysis_approval.R"))) return(current); parent <- dirname(current); if (identical(parent, current)) stop("project root not found"); current <- parent } }
project_dir <- find_root(getwd()); helper_dir <- file.path(project_dir, ".codex", "study-mmrm-analysis", "R")
for (helper in c("study_paths.R", "io.R", "specification.R", "canonical_hash.R", "standard_contract.R", "standard_analysis_definition.R", "analysis_plan.R", "analysis_contract_generation.R", "standard_sas.R", "standard_engine.R", "standard_artifacts.R", "analysis_approval.R", "case_summary.R")) source(file.path(helper_dir, helper), encoding = "UTF-8")
source(file.path(project_dir, ".codex", "study-mmrm-analysis", "scripts", "check_analysis_plan.R"), encoding = "UTF-8")
plan <- synthetic_analysis_plan(); approval <- list(review_file = "synthetic/statistical-review.md", review_sha256 = paste(rep("A", 64L), collapse = ""), analysis_plan_file = "synthetic/analysis-plan.yaml", analysis_plan_sha256 = analysis_plan_sha256(plan), approval_payload_sha256 = paste(rep("B", 64L), collapse = ""), source_evidence_sha256 = paste(rep("C", 64L), collapse = ""), reviewed_by = "Synthetic Reviewer", approved_at_utc = "2026-08-19T00:00:00Z")
contract <- compile_analysis_plan_contract(plan, approval); assert_plan_contract_parity(plan, contract); mismatched_contract <- contract; mismatched_contract$study$study_id <- "other-study"; stopifnot(inherits(try(assert_plan_contract_parity(plan, mismatched_contract), silent = TRUE), "try-error"))
study_dir <- tempfile("zz_analysis_approval_", tmpdir = project_dir); dir.create(file.path(study_dir, "analysis", "r"), recursive = TRUE); dir.create(file.path(study_dir, "analysis", "sas"), recursive = TRUE); dir.create(file.path(study_dir, "backup-trace"), recursive = TRUE); on.exit(unlink(study_dir, recursive = TRUE, force = TRUE), add = TRUE)
contract_path <- tempfile("contract-", fileext = ".yaml"); on.exit(unlink(contract_path), add = TRUE); write_standard_mmrm_contract(contract, contract_path); contract_read <- read_standard_mmrm_contract(contract_path)
rendered <- analysis_render_generated(study_dir, project_dir, contract_read, approval$approval_payload_sha256, attr(contract_read, "sha256")); stopifnot(length(rendered) == 3L, all(!vapply(rendered, function(x) grepl("<APPROVAL_PAYLOAD_SHA256>|<CONTRACT_SHA256>|specification_sha256", paste(x, collapse = "\n")), logical(1))))
sas <- paste(rendered[[grep("_template[.]sas$", names(rendered), value = TRUE)[[1L]]]], collapse = "\n"); expected_sas_hashes <- c(approval$review_sha256, approval$analysis_plan_sha256, approval$approval_payload_sha256, attr(contract_read, "sha256")); stopifnot(all(vapply(expected_sas_hashes, function(x) grepl(toupper(x), sas, fixed = TRUE), logical(1))))
generator <- paste(readLines(file.path(project_dir, ".codex", "study-mmrm-analysis", "scripts", "generate_standard_study.R"), warn = FALSE), collapse = "\n"); stopifnot(grepl("approve_and_generate_analysis.R", generator, fixed = TRUE), !grepl("analysis_transaction_publish", generator, fixed = TRUE))
prior_contract <- contract_read; second_analysis <- prior_contract$analyses[[1L]]; second_analysis$analysis_id <- "MMRM-02"; second_analysis$tfl_id <- "T14-02"; second_analysis$title <- "Removed synthetic analysis"; second_analysis$output <- list(raw_file = "MMRM-02_raw.csv", final_file = "MMRM-02_final.csv"); prior_contract$analyses <- c(prior_contract$analyses, list(second_analysis)); validate_standard_mmrm_contract(prior_contract)
prior_rendered <- analysis_render_generated(study_dir, project_dir, prior_contract, approval$approval_payload_sha256, attr(contract_read, "sha256")); analysis_transaction_publish(prior_rendered, analysis_approval_journal_path(study_dir)); obsolete <- analysis_obsolete_generated_targets(study_dir, prior_contract, rendered); stopifnot(length(obsolete) == 2L, all(file.exists(obsolete)))
delete_rollback <- try(analysis_transaction_publish(rendered, analysis_approval_journal_path(study_dir), fail_after = length(rendered), delete_targets = obsolete), silent = TRUE); stopifnot(inherits(delete_rollback, "try-error"), all(file.exists(obsolete))); analysis_transaction_publish(rendered, analysis_approval_journal_path(study_dir), delete_targets = obsolete); stopifnot(all(!file.exists(obsolete)))
paths <- study_paths(file.path(study_dir, "analysis", "r", "run_all_mmrm.R")); dir.create(paths$output_dir, recursive = TRUE); write_utf8_bom_csv(data.frame(tfl_id = "T14-01", tfl_type = "table", title = "Synthetic non-default analysis", scope_status = "approved", output_status = "blocked_mapping", raw_output_file = "", final_tfl_file = "", log_file = "", qc_file = "", note = "synthetic", stringsAsFactors = FALSE), paths$output_manifest)
chain <- list(contract = contract_read, review = list(sha256 = approval$review_sha256), plan = structure(list(), sha256 = approval$analysis_plan_sha256), approval_payload_sha256 = approval$approval_payload_sha256, contract_sha256 = attr(contract_read, "sha256"))
stopifnot(inherits(try(standard_case_summary(paths, chain), silent = TRUE), "try-error"))
cs_analysis <- standard_contract_get_analysis(contract_read, "MMRM-01"); cs_paths <- analysis_output_paths(paths, "MMRM-01")
invisible(lapply(cs_paths[c("root", "tables", "diagnostics", "logs", "models")], dir.create, recursive = TRUE, showWarnings = FALSE))
cs_rel <- function(p) project_relative_path(p, paths$project_dir)
cs_raw_path <- file.path(cs_paths$tables, cs_analysis$output$raw_file); cs_final_path <- file.path(cs_paths$tables, cs_analysis$output$final_file)
write_utf8_bom_csv(standard_raw_result_schema(), cs_raw_path); write_utf8_bom_csv(standard_final_result_schema(), cs_final_path)
writeLines("# synthetic diagnostic report", cs_paths$diagnostic_report); writeLines("synthetic log", cs_paths$log_file)
cs_diag <- standard_diagnostic_schema()[0, ]; cs_diag[1, ] <- data.frame(invocation_id = "synthetic-invocation", run_id = "synthetic-current", study_id = contract_read$study$study_id, analysis_id = "MMRM-01", analysis_group_id = "ALL", profile_version = standard_mmrm_profile_version(), review_sha256 = toupper(approval$review_sha256), analysis_plan_sha256 = toupper(approval$analysis_plan_sha256), approval_payload_sha256 = toupper(approval$approval_payload_sha256), contract_sha256 = toupper(attr(contract_read, "sha256")), covariance_path = "not_fitted", final_covariance = "", fallback_used = "no", convergence_status = "not_assessed", inference_complete = "no", failure_domain = "data_mapping", failure_phase = "endpoint_allocation_gate", computational_risk = "Not assessed", risk_reason = "synthetic blocked run", warning_summary = "", input_rows = 0L, population_filtered_rows = 0L, analysis_rows = 0L, missing_required_rows = 0L, subject_count = 0L, visit_level_count = 0L, treatment_level_count = 0L, run_status = "blocked_mapping", model_file = "", log_file = cs_rel(cs_paths$log_file), stringsAsFactors = FALSE); write_utf8_bom_csv(cs_diag, cs_paths$diagnostic_csv)
write_utf8_bom_csv(standard_build_recode_audit(list(), list()), cs_paths$recode_audit)
cs_record <- standard_run_record_schema()[0, ]; cs_record[1, ] <- list("synthetic-invocation", "synthetic-current", contract_read$study$study_id, "MMRM-01", standard_mmrm_profile_version(), toupper(approval$review_sha256), toupper(approval$analysis_plan_sha256), toupper(approval$approval_payload_sha256), toupper(attr(contract_read, "sha256")), "T14-01", "table", cs_analysis$title, "approved", "blocked_mapping", "Not assessed", cs_rel(cs_raw_path), cs_rel(cs_final_path), cs_rel(cs_paths$diagnostic_csv), cs_rel(cs_paths$diagnostic_report), cs_rel(cs_paths$log_file)); write_utf8_bom_csv(cs_record, cs_paths$run_record)
summary <- standard_case_summary(paths, chain); stopifnot(identical(summary$review_sha256, toupper(approval$review_sha256)), identical(summary$analysis_plan_sha256, toupper(approval$analysis_plan_sha256)), identical(summary$approval_payload_sha256, toupper(approval$approval_payload_sha256)), identical(summary$contract_sha256, toupper(attr(contract_read, "sha256"))), length(summary$diagnostic_metadata) == 1L)
# Missing diagnostics must now fail (no bypass); missing recode audit must fail; stale identity must fail.
invisible(file.rename(cs_paths$diagnostic_csv, paste0(cs_paths$diagnostic_csv, ".bak"))); stopifnot(inherits(try(standard_case_summary(paths, chain), silent = TRUE), "try-error")); invisible(file.rename(paste0(cs_paths$diagnostic_csv, ".bak"), cs_paths$diagnostic_csv))
invisible(file.rename(cs_paths$recode_audit, paste0(cs_paths$recode_audit, ".bak"))); stopifnot(inherits(try(standard_case_summary(paths, chain), silent = TRUE), "try-error")); invisible(file.rename(paste0(cs_paths$recode_audit, ".bak"), cs_paths$recode_audit))
stopifnot(!inherits(try(standard_case_summary(paths, chain), silent = TRUE), "try-error"))
stale_record <- cs_record; stale_record$review_sha256 <- paste(rep("F", 64L), collapse = ""); write_utf8_bom_csv(stale_record, cs_paths$run_record); stopifnot(inherits(try(standard_case_summary(paths, chain), silent = TRUE), "try-error")); stale_record <- cs_record; stale_record$study_id <- "wrong-study"; write_utf8_bom_csv(stale_record, cs_paths$run_record); stopifnot(inherits(try(standard_case_summary(paths, chain), silent = TRUE), "try-error")); write_utf8_bom_csv(cs_record, cs_paths$run_record)
first <- file.path(study_dir, "analysis", "r", "WRAP-01.R"); second <- file.path(study_dir, "analysis", "r", "WRAP-02.R"); writeLines("old-first", first); writeLines("old-second", second)
txn_dir <- analysis_approval_transaction_dir(study_dir); dir.create(txn_dir, recursive = TRUE, showWarnings = FALSE); txn_id <- "txn-test-000000000000-1-00112233445566778899aabbccddeeff"
txn_file <- function(kind) file.path(txn_dir, paste0(txn_id, ".WRAP.R.", kind, "-", basename(tempfile(""))))
failed <- try(analysis_transaction_publish(setNames(list("new-first", "new-second"), c(first, second)), analysis_approval_journal_path(study_dir), fail_after = 1L), silent = TRUE); stopifnot(inherits(failed, "try-error"), identical(readLines(first), "old-first"), identical(readLines(second), "old-second"), !file.exists(analysis_approval_journal_path(study_dir)))
missing_backup <- txn_file("backup"); missing_stage <- txn_file("stage"); writeLines("published-first", first); missing_entries <- list(list(target = first, stage = missing_stage, backup = missing_backup, existed = TRUE, delete = FALSE)); analysis_approval_write_journal(analysis_approval_journal_path(study_dir), "committing", missing_entries, txn_id); blocked_recovery <- try(analysis_approval_recover(study_dir), silent = TRUE); stopifnot(inherits(blocked_recovery, "try-error"), identical(readLines(first), "published-first"), file.exists(analysis_approval_journal_path(study_dir))); writeLines("old-first", missing_backup); stopifnot(analysis_approval_recover(study_dir), identical(readLines(first), "old-first"))
backup_first <- txn_file("backup"); backup_second <- txn_file("backup"); stage_first <- txn_file("stage"); stage_second <- txn_file("stage"); writeLines("old-first", backup_first); writeLines("old-second", backup_second); writeLines("unused", stage_first); writeLines("unused", stage_second); writeLines("old-first", first); writeLines("published-second", second); rolling_entries <- list(list(target = first, stage = stage_first, backup = backup_first, existed = TRUE, delete = FALSE), list(target = second, stage = stage_second, backup = backup_second, existed = TRUE, delete = FALSE)); analysis_approval_write_journal(analysis_approval_journal_path(study_dir), "rolling_back", rolling_entries, txn_id); stopifnot(analysis_approval_recover(study_dir), identical(readLines(first), "old-first"), identical(readLines(second), "old-second"), !file.exists(backup_first), !file.exists(backup_second))
completed_backup <- txn_file("backup"); completed_stage <- txn_file("stage"); writeLines("old-first", completed_backup); writeLines("unused", completed_stage); writeLines("published-first", first); completed_entries <- list(list(target = first, stage = completed_stage, backup = completed_backup, existed = TRUE, delete = FALSE)); analysis_approval_write_journal(analysis_approval_journal_path(study_dir), "completed", completed_entries, txn_id); stopifnot(analysis_approval_recover(study_dir), identical(readLines(first), "published-first"), !file.exists(completed_backup), !file.exists(completed_stage))
# Malicious/out-of-bounds journal traps: recovery must fail closed and touch nothing.
journal_path <- analysis_approval_journal_path(study_dir)
outside_target <- tempfile("zz_outside_target_", tmpdir = project_dir, fileext = ".txt"); writeLines("precious", outside_target)
trap_backup <- txn_file("backup"); writeLines("attacker", trap_backup)
trap_target <- list(list(target = outside_target, stage = "", backup = trap_backup, existed = TRUE, delete = TRUE)); analysis_approval_write_journal(journal_path, "committing", trap_target, txn_id)
stopifnot(inherits(try(analysis_approval_recover(study_dir), silent = TRUE), "try-error"), file.exists(outside_target), identical(readLines(outside_target), "precious"), file.exists(trap_backup), file.exists(journal_path)); unlink(c(journal_path, outside_target), force = TRUE)
evil_backup <- tempfile("zz_evil_backup_", tmpdir = project_dir, fileext = ".txt"); writeLines("evil", evil_backup)
trap_stage_backup <- list(list(target = first, stage = "", backup = evil_backup, existed = TRUE, delete = TRUE)); analysis_approval_write_journal(journal_path, "committing", trap_stage_backup, txn_id)
stopifnot(inherits(try(analysis_approval_recover(study_dir), silent = TRUE), "try-error"), file.exists(evil_backup), identical(readLines(evil_backup), "evil"), identical(readLines(first), "published-first")); unlink(c(journal_path, evil_backup), force = TRUE)
trap_traversal <- list(list(target = file.path(study_dir, "analysis", "r", "..", "..", "escape.R"), stage = txn_file("stage"), backup = "", existed = FALSE, delete = FALSE)); analysis_approval_write_journal(journal_path, "committing", trap_traversal, txn_id)
stopifnot(inherits(try(analysis_approval_recover(study_dir), silent = TRUE), "try-error")); unlink(journal_path, force = TRUE)
yaml::write_yaml(list(schema_version = "1.1", phase = "committing", transaction_id = txn_id, entries = list(), updated_at_utc = "2026-08-19T00:00:00Z", malicious = "x"), journal_path)
stopifnot(inherits(try(analysis_approval_recover(study_dir), silent = TRUE), "try-error")); unlink(journal_path, force = TRUE)
yaml::write_yaml(list(schema_version = "1.0", phase = "committing", transaction_id = txn_id, entries = list(), updated_at_utc = "2026-08-19T00:00:00Z"), journal_path)
stopifnot(inherits(try(analysis_approval_recover(study_dir), silent = TRUE), "try-error")); unlink(journal_path, force = TRUE)
# Removed-analysis output roots are transactionally deleted on reapproval; rollback restores them.
removed_root <- analysis_output_paths(paths, "MMRM-02")$root; dir.create(file.path(removed_root, "tables"), recursive = TRUE, showWarnings = FALSE); dir.create(file.path(removed_root, "diagnostics"), recursive = TRUE, showWarnings = FALSE)
removed_run_record <- file.path(removed_root, "analysis-run-record.csv"); removed_table <- file.path(removed_root, "tables", "MMRM-02_final.csv"); writeLines("stale-run-record", removed_run_record); writeLines("stale-table", removed_table)
removed_ids <- analysis_removed_analysis_ids(prior_contract, contract_read); stopifnot(identical(removed_ids, "MMRM-02")); removed_files <- analysis_removed_output_files(study_dir, removed_ids); norm <- function(x) normalizePath(x, winslash = "/", mustWork = FALSE); stopifnot(setequal(norm(removed_files), norm(c(removed_run_record, removed_table))))
removed_rollback <- try(analysis_transaction_publish(setNames(list("x"), file.path(study_dir, "analysis", "r", "WRAP-03.R")), analysis_approval_journal_path(study_dir), fail_after = 0L, delete_targets = removed_files), silent = TRUE); stopifnot(inherits(removed_rollback, "try-error"), file.exists(removed_run_record), file.exists(removed_table))
analysis_transaction_publish(setNames(list("x"), file.path(study_dir, "analysis", "r", "WRAP-03.R")), analysis_approval_journal_path(study_dir), delete_targets = removed_files); stopifnot(!file.exists(removed_run_record), !file.exists(removed_table))
dir.create(file.path(study_dir, "statistician-review"), recursive = TRUE); writeLines("LEGACY_TRAP", file.path(study_dir, "statistician-review", "analysis-specification.md")); stopifnot(inherits(try(reject_legacy_analysis_artifacts(study_dir), silent = TRUE), "try-error")); unlink(file.path(study_dir, "statistician-review", "analysis-specification.md")); writeLines("LEGACY_ENDPOINT_TRAP", file.path(study_dir, "statistician-review", "endpoint-mapping.yaml")); stopifnot(inherits(try(reject_legacy_analysis_artifacts(study_dir), silent = TRUE), "try-error"))
unlink(study_dir, recursive = TRUE, force = TRUE)
cat("Approval generation rendering, crash recovery, identity parity, case-summary provenance, SAS trace, single-publisher, and legacy trap checks passed.\n")
